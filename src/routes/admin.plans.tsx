import { createFileRoute } from "@tanstack/react-router"; import { WorkspacePage } from "@/components/workspace-shell"; import { pageMeta } from "@/lib/meta";
export const Route=createFileRoute("/admin/plans")({head:()=>pageMeta("Plan administration","Manage Zeruvo plans."),component:()=> <WorkspacePage title="Plans" section="A01" admin/>});