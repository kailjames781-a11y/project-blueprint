import { createFileRoute } from "@tanstack/react-router"; import { WorkspacePage } from "@/components/workspace-shell"; import { pageMeta } from "@/lib/meta";
export const Route=createFileRoute("/admin/roles")({head:()=>pageMeta("Role administration","Manage administrative roles."),component:()=> <WorkspacePage title="Roles" section="A09" admin/>});