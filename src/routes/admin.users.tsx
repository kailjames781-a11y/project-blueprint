import { createFileRoute } from "@tanstack/react-router"; import { WorkspacePage } from "@/components/workspace-shell"; import { pageMeta } from "@/lib/meta";
export const Route=createFileRoute("/admin/users")({head:()=>pageMeta("User administration","Manage Zeruvo users."),component:()=> <WorkspacePage title="Users" section="A02" admin/>});