import { createFileRoute } from "@tanstack/react-router"; import { WorkspacePage } from "@/components/workspace-shell"; import { pageMeta } from "@/lib/meta";
export const Route=createFileRoute("/dashboard/connections")({head:()=>pageMeta("Connections","Manage future repository connections."),component:()=> <WorkspacePage title="Connections" section="05"/>});